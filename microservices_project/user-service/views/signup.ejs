<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <style>
      .card {
        background: linear-gradient(135deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0.02) 100%);
        backdrop-filter: blur(10px);
        border-color: rgba(255,255,255,0.1);
        box-shadow: 0 8px 32px rgba(0,0,0,0.3);
      }
      input:focus {
        outline: none;
        border-color: rgb(59, 130, 246);
        box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
      }
      input[type="submit"]:hover {
        background-color: rgb(37, 99, 235);
        box-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
      }
    </style>
  </head>
  <body class="w-full min-h-screen bg-gradient-to-br from-zinc-950 via-zinc-900 to-black text-white p-10 flex flex-col items-center justify-center">
    <div class="text-center mb-8">
      <h1 class="text-4xl font-bold mb-2">Create Account</h1>
    </div>
    <div class="card p-10 border w-full max-w-md rounded-lg">
      <form action="/user/createUser" method="post" class="flex flex-col gap-4">
        <input class="bg-transparent border rounded-md px-4 py-3 border-zinc-500 focus:border-blue-500 transition duration-200" type="text" placeholder="Username" name="username" required>
        <input class="bg-transparent border rounded-md px-4 py-3 border-zinc-500 focus:border-blue-500 transition duration-200" type="email" placeholder="Email" name="email" required>
        <input class="bg-transparent border rounded-md px-4 py-3 border-zinc-500 focus:border-blue-500 transition duration-200" type="password" placeholder="Password" name="password" required>
        <input type="submit" value="Create Account" class="border border-blue-500 cursor-pointer bg-blue-500 px-5 py-3 rounded-md font-semibold hover:bg-blue-600 transition duration-200 mt-2">
      </form>
    </div>
  </body>
</html>

