<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <div>
      <a href="/logout" class="px-3 py-2 bg-red-500 rounded-md block ms-auto w-min">Logout</a>
    </div>
    <span class="block">edit your post</span>
    <form action="/post/update/<%= post._id %>" method="post">
      <textarea class="border rounded-md border-zinc-500 mt-3 p-3 resize-none w-1/3" name="content" placeholder="What's on your mind ?"><%= post.content %></textarea>
      <input class="bg-yellow-500 text-black px-5 py-2 rounded-md block" type="submit" value="Update Post">
    </form>
  </body>
</html>
