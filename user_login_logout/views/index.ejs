<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <h3 class="text-3xl pb-4">Sign Up</h3>
        <form method="post" action="/create">
            <input class="px-3 py-2 border-2 rounded-md bg-transparent border-zinc-800 outline-none" type="text" name="username" placeholder="username">
            <input class="px-3 py-2 border-2 rounded-md bg-transparent border-zinc-800 outline-none" type="email" name="email" placeholder="email">
            <input class="px-3 py-2 border-2 rounded-md bg-transparent border-zinc-800 outline-none" type="password" name="password" placeholder="password">
            <input class="px-3 py-2 border-2 rounded-md bg-transparent border-zinc-800 outline-none" type="number" name="age" placeholder="age">
            <input class="px-4 py-2 rounded-md bg-blue-500" type="submit" value="Sign up">
        </form>
    </div>
</body>
</html>